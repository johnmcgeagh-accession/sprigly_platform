---
phase: 01-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/johnmcgeagh/Documents/Workspaces/sprigly/site/**
  - /Users/johnmcgeagh/Documents/Workspaces/sprigly/engine/package.json
  - /Users/johnmcgeagh/Documents/Workspaces/sprigly/engine/pnpm-workspace.yaml
  - /Users/johnmcgeagh/Documents/Workspaces/sprigly/engine/turbo.json
  - /Users/johnmcgeagh/Documents/Workspaces/sprigly/engine/tsconfig.json
  - /Users/johnmcgeagh/Documents/Workspaces/sprigly/engine/.env.example
  - /Users/johnmcgeagh/Documents/Workspaces/sprigly/engine/.gitignore
  - /Users/johnmcgeagh/Documents/Workspaces/sprigly/engine/apps/**
  - /Users/johnmcgeagh/Documents/Workspaces/sprigly/engine/packages/**
autonomous: true
---

<objective>
## Goal
Create the `sprigly/` workspace, migrate the existing Sprigly marketing site, and scaffold the full monorepo skeleton — all root configs plus correct `package.json`, `tsconfig.json`, and `src/index.ts` stubs for every app and package. No business logic yet.

## Purpose
Everything in Phases 2–4 depends on the monorepo being correctly wired. pnpm workspaces must resolve, Turborepo pipeline must be valid, TypeScript must compile across all packages, and inter-package `@sprigly/*` imports must work before any implementation begins.

## Output
- `/Users/johnmcgeagh/Documents/Workspaces/sprigly/site/` — copy of current Sprigly marketing site
- `/Users/johnmcgeagh/Documents/Workspaces/sprigly/engine/` — full monorepo skeleton
- `pnpm install` runs clean with zero errors
- `pnpm build` (turbo) completes for all stub packages
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md

## Source: Existing Sprigly Marketing Site
Source path: /Users/johnmcgeagh/Documents/Workspaces/aigura/dev/sprigly_website/sprigly-site/
Destination: /Users/johnmcgeagh/Documents/Workspaces/sprigly/site/
Copy only — do NOT delete from the aigura workspace.
</context>

<acceptance_criteria>

## AC-1: Workspace structure exists
```gherkin
Given the task completes
When you run: ls /Users/johnmcgeagh/Documents/Workspaces/sprigly/
Then you see both: engine/  site/
```

## AC-2: Marketing site copied intact
```gherkin
Given the site copy task runs
When you run: ls /Users/johnmcgeagh/Documents/Workspaces/sprigly/site/
Then you see the standard Next.js files: app/  components/  package.json  next.config.mjs  (etc.)
And the original at aigura/dev/sprigly_website/sprigly-site/ is unchanged
```

## AC-3: Monorepo installs cleanly
```gherkin
Given the scaffold is complete
When you run: cd /Users/johnmcgeagh/Documents/Workspaces/sprigly/engine && pnpm install
Then the command exits 0 with no errors
And node_modules/.pnpm exists
And all @sprigly/* packages are linked in the workspace
```

## AC-4: All packages and apps present
```gherkin
Given pnpm install succeeds
When you run: ls packages/ && ls apps/
Then packages/ shows: db  model-client  engine  sources  workflows  destinations  oauth-tokens  audit  prompts  ui
And apps/ shows: web  worker
```

## AC-5: TypeScript compiles across all packages
```gherkin
Given the scaffold is complete
When you run: pnpm build (or tsc --noEmit for each package)
Then compilation succeeds with zero errors across all packages and apps
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Create workspace structure and copy marketing site</name>
  <files>
    /Users/johnmcgeagh/Documents/Workspaces/sprigly/site/ (directory + contents)
    /Users/johnmcgeagh/Documents/Workspaces/sprigly/engine/ (empty directory)
  </files>
  <action>
    1. The sprigly/ directory and .paul/ already exist (created by plan phase).
       Create the engine/ subdirectory:
       mkdir -p /Users/johnmcgeagh/Documents/Workspaces/sprigly/engine

    2. Copy the existing Sprigly marketing site (do NOT delete the source):
       cp -r /Users/johnmcgeagh/Documents/Workspaces/aigura/dev/sprigly_website/sprigly-site/. \
             /Users/johnmcgeagh/Documents/Workspaces/sprigly/site/

    3. Remove node_modules from the copy (they'll reinstall from package.json):
       rm -rf /Users/johnmcgeagh/Documents/Workspaces/sprigly/site/node_modules
       rm -f  /Users/johnmcgeagh/Documents/Workspaces/sprigly/site/package-lock.json

    Do NOT touch the aigura workspace originals.
  </action>
  <verify>
    ls /Users/johnmcgeagh/Documents/Workspaces/sprigly/
    → shows: engine/  site/  .paul/

    ls /Users/johnmcgeagh/Documents/Workspaces/sprigly/site/
    → shows Next.js files (app/, components/, package.json, next.config.mjs, ...)

    ls /Users/johnmcgeagh/Documents/Workspaces/aigura/dev/sprigly_website/sprigly-site/
    → still shows original files (unchanged)
  </verify>
  <done>AC-1 and AC-2 satisfied</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold monorepo root config and all package/app stubs</name>
  <files>
    engine/package.json
    engine/pnpm-workspace.yaml
    engine/turbo.json
    engine/tsconfig.json
    engine/.env.example
    engine/.gitignore
    engine/apps/web/package.json, tsconfig.json, next.config.ts, app/layout.tsx, app/page.tsx
    engine/apps/worker/package.json, tsconfig.json, src/index.ts
    engine/packages/db/package.json, tsconfig.json, src/index.ts
    engine/packages/model-client/package.json, tsconfig.json, src/index.ts
    engine/packages/engine/package.json, tsconfig.json, src/index.ts, src/types.ts
    engine/packages/sources/package.json, tsconfig.json, src/index.ts
    engine/packages/workflows/package.json, tsconfig.json, src/index.ts
    engine/packages/destinations/package.json, tsconfig.json, src/index.ts
    engine/packages/oauth-tokens/package.json, tsconfig.json, src/index.ts
    engine/packages/audit/package.json, tsconfig.json, src/index.ts
    engine/packages/prompts/package.json, tsconfig.json, src/index.ts
    engine/packages/ui/package.json, tsconfig.json, src/index.ts
  </files>
  <action>
    All paths below are relative to /Users/johnmcgeagh/Documents/Workspaces/sprigly/engine/

    ── Root config ──────────────────────────────────────────

    package.json (workspace root):
    {
      "name": "sprigly",
      "private": true,
      "packageManager": "pnpm@9.0.0",
      "engines": { "node": ">=20" },
      "scripts": {
        "build": "turbo build",
        "dev": "turbo dev",
        "lint": "turbo lint",
        "test": "turbo test",
        "type-check": "turbo type-check"
      },
      "devDependencies": {
        "turbo": "^2.0.0",
        "typescript": "^5.4.0",
        "@types/node": "^20.0.0"
      }
    }

    pnpm-workspace.yaml:
    packages:
      - "apps/*"
      - "packages/*"

    turbo.json:
    {
      "$schema": "https://turbo.build/schema.json",
      "globalEnv": [
        "DATABASE_URL", "NODE_ENV",
        "ANTHROPIC_API_KEY",
        "AWS_REGION", "AWS_ACCESS_KEY_ID", "AWS_SECRET_ACCESS_KEY", "AWS_KMS_KEY_ID",
        "MODEL_PROVIDER",
        "REDIS_URL",
        "NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY", "CLERK_SECRET_KEY",
        "GOOGLE_CLIENT_ID", "GOOGLE_CLIENT_SECRET",
        "LOCAL_DEV_ENCRYPTION_KEY"
      ],
      "tasks": {
        "build": {
          "dependsOn": ["^build"],
          "outputs": [".next/**", "dist/**", "!.next/cache/**"]
        },
        "dev": {
          "cache": false,
          "persistent": true
        },
        "lint": { "dependsOn": ["^lint"] },
        "test": { "dependsOn": ["^build"] },
        "type-check": { "dependsOn": ["^build"] }
      }
    }

    tsconfig.json (base, at root — all packages extend this):
    {
      "compilerOptions": {
        "target": "ES2022",
        "module": "NodeNext",
        "moduleResolution": "NodeNext",
        "lib": ["ES2022"],
        "strict": true,
        "noUncheckedIndexedAccess": true,
        "exactOptionalPropertyTypes": true,
        "noImplicitOverride": true,
        "declaration": true,
        "declarationMap": true,
        "sourceMap": true,
        "skipLibCheck": true,
        "esModuleInterop": true,
        "resolveJsonModule": true,
        "outDir": "dist",
        "rootDir": "src"
      },
      "exclude": ["node_modules", "dist"]
    }

    .gitignore:
    node_modules/
    dist/
    .next/
    .turbo/
    *.env
    .env.local
    .env.*.local
    *.log

    .env.example:
    # ── Database ──────────────────────────────────────────────
    DATABASE_URL=postgresql://user:password@localhost:5432/sprigly

    # ── Auth (Clerk) ───────────────────────────────────────────
    NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
    CLERK_SECRET_KEY=

    # ── Model Provider ─────────────────────────────────────────
    # Set to "anthropic" for local dev, "bedrock" for production
    MODEL_PROVIDER=anthropic

    # Anthropic (dev)
    ANTHROPIC_API_KEY=

    # AWS Bedrock (prod) ── set up via Plan 01-04 checkpoint
    AWS_REGION=eu-west-2
    AWS_ACCESS_KEY_ID=
    AWS_SECRET_ACCESS_KEY=
    # Bedrock cross-region inference profile model IDs
    BEDROCK_MODEL_SONNET=eu.anthropic.claude-sonnet-4-6-20251031-v1:0
    BEDROCK_MODEL_OPUS=eu.anthropic.claude-opus-4-7-20250514-v1:0

    # ── Encryption ─────────────────────────────────────────────
    # KMS master key ARN (prod) ── set up via Plan 01-04 checkpoint
    AWS_KMS_KEY_ID=
    # Fallback for local dev if no AWS creds present
    LOCAL_DEV_ENCRYPTION_KEY=change-me-32-chars-minimum-please

    # ── Redis / BullMQ ─────────────────────────────────────────
    REDIS_URL=redis://localhost:6379

    # ── Gmail OAuth ────────────────────────────────────────────
    GOOGLE_CLIENT_ID=
    GOOGLE_CLIENT_SECRET=
    # Redirect URI registered in Google Console:
    GOOGLE_OAUTH_REDIRECT_URI=http://localhost:3000/api/oauth/gmail/callback

    # ── Seed values ────────────────────────────────────────────
    # UUIDs used by the seed script — generate once and keep stable
    SPRIGLY_CLIENT_ID=
    ADMIN_USER_EMAIL=john@sprigly.co.uk

    ── Packages ─────────────────────────────────────────────────

    For each of the 10 packages, create the following structure.
    Substitute {name} and {scope} per package (listed below).

    packages/{name}/package.json:
    {
      "name": "@sprigly/{name}",
      "version": "0.0.1",
      "private": true,
      "type": "module",
      "main": "./dist/index.js",
      "types": "./dist/index.d.ts",
      "exports": {
        ".": {
          "import": "./dist/index.js",
          "types": "./dist/index.d.ts"
        }
      },
      "scripts": {
        "build": "tsc",
        "dev": "tsc --watch",
        "type-check": "tsc --noEmit",
        "test": "vitest run"
      },
      "devDependencies": {
        "typescript": "*",
        "vitest": "^1.6.0"
      }
    }

    packages/{name}/tsconfig.json:
    {
      "extends": "../../tsconfig.json",
      "compilerOptions": {
        "outDir": "dist",
        "rootDir": "src"
      },
      "include": ["src"]
    }

    packages/{name}/src/index.ts:
    // @sprigly/{name}
    // TODO: implement in Phase {N}
    export {};

    Package list (name → placeholder phase comment):
    - db             → Phase 1, Plan 02
    - model-client   → Phase 1, Plan 03
    - audit          → Phase 1, Plan 03
    - oauth-tokens   → Phase 1, Plan 04
    - engine         → Phase 2, Plan 01
    - sources        → Phase 2, Plan 02
    - workflows      → Phase 2, Plan 03
    - destinations   → Phase 2, Plan 04
    - prompts        → Phase 2, Plan 01
    - ui             → Phase 3, Plan 01

    Special case — packages/engine also needs src/types.ts:
    // Core engine types — implemented in Phase 2 Plan 01
    // Source: PROJECT.md types section
    export type SourceType = "email" | "sms" | "slack" | "form" | "voice" | "webhook" | "schedule";
    export interface IncomingEvent { id: string; clientId: string; source: SourceType; [key: string]: unknown }
    export interface Workflow<TInput = unknown, TOutput = unknown> { id: string; }
    export interface Destination<TOutput = unknown> { id: string; }
    // Full types implemented in Phase 2

    ── Apps ─────────────────────────────────────────────────────

    apps/worker/package.json:
    {
      "name": "@sprigly/worker",
      "version": "0.0.1",
      "private": true,
      "type": "module",
      "scripts": {
        "build": "tsc",
        "dev": "tsx watch src/index.ts",
        "start": "node dist/index.js",
        "type-check": "tsc --noEmit"
      },
      "dependencies": {
        "pino": "^9.0.0"
      },
      "devDependencies": {
        "typescript": "*",
        "@types/node": "*",
        "tsx": "^4.0.0"
      }
    }

    apps/worker/tsconfig.json:
    {
      "extends": "../../tsconfig.json",
      "compilerOptions": {
        "outDir": "dist",
        "rootDir": "src"
      },
      "include": ["src"]
    }

    apps/worker/src/index.ts:
    // Sprigly Worker — entry point
    // Implemented in Phase 2, Plan 05
    import pino from 'pino';
    const logger = pino({ name: 'sprigly-worker' });
    logger.info('Worker starting (stub)');

    apps/web/package.json:
    {
      "name": "@sprigly/web",
      "version": "0.0.1",
      "private": true,
      "scripts": {
        "build": "next build",
        "dev": "next dev --port 3100",
        "start": "next start",
        "lint": "next lint",
        "type-check": "tsc --noEmit"
      },
      "dependencies": {
        "next": "14.2.0",
        "react": "^18.3.0",
        "react-dom": "^18.3.0",
        "@clerk/nextjs": "^6.0.0"
      },
      "devDependencies": {
        "typescript": "*",
        "@types/node": "*",
        "@types/react": "*",
        "@types/react-dom": "*",
        "tailwindcss": "^3.4.0",
        "autoprefixer": "^10.4.0",
        "postcss": "^8.4.0"
      }
    }

    apps/web/tsconfig.json:
    {
      "extends": "../../tsconfig.json",
      "compilerOptions": {
        "module": "ESNext",
        "moduleResolution": "Bundler",
        "jsx": "preserve",
        "plugins": [{ "name": "next" }],
        "paths": { "@/*": ["./src/*"] }
      },
      "include": ["next-env.d.ts", "src/**/*", ".next/types/**/*.ts"],
      "exclude": ["node_modules"]
    }

    apps/web/next.config.ts:
    import type { NextConfig } from 'next';
    const config: NextConfig = { experimental: {} };
    export default config;

    apps/web/src/app/layout.tsx:
    import type { Metadata } from 'next';
    export const metadata: Metadata = { title: 'Sprigly Admin' };
    export default function RootLayout({ children }: { children: React.ReactNode }) {
      return <html lang="en"><body>{children}</body></html>;
    }

    apps/web/src/app/page.tsx:
    export default function HomePage() {
      return <main><p>Sprigly Admin — coming in Phase 3</p></main>;
    }

    ── Final step ───────────────────────────────────────────────

    From /Users/johnmcgeagh/Documents/Workspaces/sprigly/engine/:
    Run: pnpm install

    This links all @sprigly/* workspace packages. Fix any resolution errors before marking complete.
  </action>
  <verify>
    cd /Users/johnmcgeagh/Documents/Workspaces/sprigly/engine
    pnpm install          # must exit 0
    ls packages/          # db  model-client  engine  sources  workflows  destinations  oauth-tokens  audit  prompts  ui
    ls apps/              # web  worker
    pnpm type-check       # tsc --noEmit across all — must exit 0
  </verify>
  <done>AC-3, AC-4, and AC-5 satisfied</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- /Users/johnmcgeagh/Documents/Workspaces/aigura/ — aigura workspace is read-only for this plan (source for site copy only)
- /Users/johnmcgeagh/Documents/Workspaces/sprigly/.paul/ — PAUL state files are managed by PAUL workflow, not by apply

## SCOPE LIMITS
- No business logic in this plan — stubs and configs only
- Do not implement any package APIs (db schema, model client, etc.) — those are Plans 01-02 through 01-04
- Do not set up Railway, Clerk, or any external services — those come later
- Do not modify the copied site/ contents — it runs independently as-is
- apps/web gets only a bare Next.js shell — no Clerk wiring, no admin pages yet

</boundaries>

<verification>
Before declaring plan complete:
- [ ] ls /Users/johnmcgeagh/Documents/Workspaces/sprigly/ shows engine/ and site/
- [ ] ls site/ shows Next.js project files (app/, components/, package.json)
- [ ] Original at aigura/dev/sprigly_website/sprigly-site/ is untouched
- [ ] cd engine && pnpm install exits 0
- [ ] ls engine/packages/ shows all 10 packages
- [ ] ls engine/apps/ shows web and worker
- [ ] pnpm type-check (or tsc --noEmit) passes across all packages
- [ ] engine/.env.example exists with all variable keys documented
</verification>

<success_criteria>
- All tasks completed with zero errors
- pnpm install succeeds — workspace links all @sprigly/* packages
- TypeScript compiles clean across all stubs
- Marketing site copied to site/ intact
- aigura workspace unchanged
</success_criteria>

<output>
After completion, create: .paul/phases/01-foundations/01-01-SUMMARY.md
</output>
