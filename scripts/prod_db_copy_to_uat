# name them explicitly so no write command ever gets the prod URL by accident
export PROD_DB="postgresql://postgres:h7wHnoYfmJ0C3FLa2hS4F3jWeaZBBV8CuQn8eXgcw8I=@yamabiko.proxy.rlwy.net:59459/railway"      # only ever read from this
export UAT_DB="postgresql://postgres:XCfaCYpUuYZlFJkkPeihBPaKKrmOrBEG@hayabusa.proxy.rlwy.net:24746/railway"        # only ever write to this

# 1. dump prod (read-only on prod) — custom format, compressed, restorable
pg_dump "$PROD_DB" -Fc -f prod_snapshot.dump

# 2. restore into UAT — --clean --if-exists so a re-run is idempotent
pg_restore --clean --if-exists --no-owner --no-privileges \
  -d "$UAT_DB" prod_snapshot.dump
