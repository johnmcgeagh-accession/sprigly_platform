/** Central config — update these when deploying or swapping integrations. */

/** Booking link. Swap this one constant to change every CTA on the site. */
export const BOOKING_URL = '/book'

/** Production site URL — used for OG tags, sitemap, JSON-LD. */
export const SITE_URL = 'https://www.sprigly.co.uk'
