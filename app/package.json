{
  "name": "@sprigly/app",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "sh -c 'set -a && . ../.env.local && set +a && next dev --port 3200'",
    "build": "next build",
    "start": "next start --port 3200",
    "lint": "next lint",
    "type-check": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "next": "^14.2.25",
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "lucide-react": "^0.400.0",
    "drizzle-orm": "^0.30.0",
    "bullmq": "^5.0.0",
    "@sprigly/db": "workspace:*",
    "@sprigly/embedding-client": "workspace:*",
    "@sprigly/knowledge": "workspace:*",
    "@sprigly/model-client": "workspace:*"
  },
  "devDependencies": {
    "typescript": "*",
    "@types/node": "*",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "vitest": "^1.6.0"
  }
}
